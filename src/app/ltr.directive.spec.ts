import { LtrDirective } from './ltr.directive';

describe('LtrDirective', () => {
  it('should create an instance', () => {
    const directive = new LtrDirective();
    expect(directive).toBeTruthy();
  });
});
